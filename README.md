# Random Test

This repository contains a tiny pytest that demonstrates deterministic behavior when seeding Python's `random` module.

Run the tests (Windows PowerShell):

```powershell
python -m pip install -U pytest
pytest -q
```
